# 最终集成验收准备（28 尚未解锁）

2026-09-25。25/27 尚在验收，本文件仅冻结后续任务与交付检查，不作为领取或完成记录。两项依赖关闭后将此计划纳入正式证据；最终源码 SHA、APK SHA-256、角色/策略哈希在构建后填入，禁止使用较早 APK 冒充最终版本。

最终同一 APK 先完成25既定中文输入/蓝框 OFF/ON 配对，不重复已经覆盖且代码不变的历史基线。保留原版手机与 AndroidWorld 两入口、提取版、裁剪版、旧桥接、独立仅VLM、Jev选择、树核验、按需规划各版本和失败。gui-plus 与日期版的跨模型差异不归因于优化。

留出运行首次采用目标“在中文输入框中输入‘今天只用手机完成任务’”，初始输入框为空；独立真值为目标输入框实际文本逐字相等，并正确判定整任务完成。此实例与开发用文字不同，但共享输入任务结构，只验证新中文目标，不声称未见应用或广泛泛化。留出首次结果无论成功失败均保存；新重试须有具体源码或环境变化，原记录不删除。最终蓝框任务沿用冻结整体判据，明确属于回归而非新留出。

最终签名 APK 安装升级保留已有加密设置和预算，不清除数据。设置界面确认 gui-plus、jev-1.13.0 及最终开关；普通使用只要求 Android权限、互联网和用户Key。交付说明包含安装、配置、开始、暂停/取消、fresh核对与明确恢复/结束、未知结果限制和更新流程，不以电脑/桥配置为前提。检查产物不包含测试Key或env。

物理拔线前，完成全部可代办的安装、系统授权、离线检查和现场准备；确认无线调试关闭、ADB reverse为空、项目编排服务停止。用户拔USB，开发机ADB server停止；运行窗口不触碰手机或通过宿主采集。用户在手机运行最终中文目标与蓝框，另在模型等待时暂停/锁屏，验证解锁不自动续跑，经通知重新观察核对并明确恢复或结束。保留任务本地记录，完成后回连一次导出，再锁屏。拔线事实及用户复核单独记载，不将USB准备/取证混称全程断线。

最终阶段新增费用与未知预留最多¥2，全程累计¥10、Jev150（2026-09-25用户明确由40→70→150提高，历史调用保留）、单任务¥1/5步/25 VLM/5 Jev不变。只有主协调操作设备、真实API、预算与Git。最终报告列出全部失败与剩余未验证范围，不以模型自述或动作回执替代实际副作用。

2026-09-25继续实施：当前Jev46/70，已计费用及未知预留¥3.484247；规划阶段¥0.5299715。复测预检在规划任务前另保留最终两项所需10次Jev余量；此安排不提高用户任何上限，不删除旧失败或费用。只有25/27前置结果满足放行且安装/配置准备就绪后，再向用户发起具体拔线操作请求。

2026-09-25追加授权：用户明确“Jev调用扩充到总共150次”。由70提高为150，已用46保留，累计¥10、每任务¥1、阶段¥2及每任务5次Jev均不变。所有历史尝试和费用继续计入。

准备期只读检查：`adb reverse --list`为空，`settings get global adb_wifi_enabled`为0，项目进程名/路径检索未发现运行中的项目Python编排服务，手机Dozing。此为准备快照，实际拔线前须再次核查并停止ADB server；不当作已完成物理拔线证据。

2026-09-25继续排障：已确认当前Jev 60/150，已计费用和未知预留累计¥3.7455340，规划阶段¥0.7912585，恢复阶段¥0.2515115，最终阶段尚未调用。无需新增额度授权；25/27的范围内缺陷继续修复，物理拔线仍等待前置修复验收完成。当前b84d1b8候选APK经apksigner verify通过，使用Android Debug测试签名，证书SHA-256为`38dd45369017a980dae905841d8f76aff025fa5e3a434ed3aa421239343194c6`；这是升级兼容性准备信息，不将测试签名候选冒充正式发布版或最终验收产物。最终APK构建后重新记录文件哈希与签名。

2026-09-26：c4a718b四项对照已归档，3项自动成功、OFF蓝框未完成；Jev66/150，已结算费用与未知预留¥3.8819830。recovery27-crossapp01另保留¥1任务预留，实际尚无请求或动作；通知栏未收起造成无可信应用，安全暂停后不能取消的产品死锁正在修复。最终拔线不提前开始，仍由主协调先修复、打包和准备。
