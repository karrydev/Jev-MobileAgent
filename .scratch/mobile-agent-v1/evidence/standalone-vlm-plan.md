# 任务 26 独立 App 验收计划

冻结日期：2026-09-25。只验证 spec/26 的手机内仅 VLM 闭环；完整恢复故障矩阵由 27 承接。当前手机 OnePlus 8T / Android 14，GUI-Plus `gui-plus-2026-02-26`，真实测试凭据由主 Agent 从用户指定 env 读取后经 App 配置；Key 不进入命令输出、APK 或报告。

## 行为与放行

1. 在 App 设置并保存 VLM/Jev 配置；单独清除 Jev 配置不应阻止仅 VLM 任务。验证掩码、替换/清除、旧明文处理、Keystore 加密及导出无 Key。测试凭据和旧测试任务记录与用户数据区分，清理仅针对已归档的本轮测试夹具。
2. 中文输入：受控输入框写入“独立手机测试成功”；独立观察检查文本精确相等，任务正确结束。
3. 视觉动作：受控自绘目标或既有视觉受控用例；以独立页面状态/可见副作用判定成功，不采用模型自述。
4. 验证切换到目标页面时本地任务继续、暂停/取消入口可达，模型迟到响应不派发。网络/非法响应/预算/usage 缺失的相关回放由实现测试提供；真机观察权限失效时不得继续操作或报告成功。
5. 实际执行窗口中停止项目 Python 服务、清除 ADB reverse、关闭无线调试，并断开 USB；执行后再连接导出手机本地报告与截图。若只能达到部分隔离，明确记为阶段验证，不关闭本票独立使用门槛。

每个正向任务首轮 1 次；失败保留原因，只有修复或环境变化后重试。每个任务最多 5 步、25 次模型请求、单请求最多 1024 输出 token，任务限额 ¥1；全部请求与重试仍受用户已批准的累计 ¥10 限制。已知标价与未知费用预留分别核算；不得通过重建任务预算掩盖累计支出。

新基线须同时具备：App 本地运行证据、真实供应商返回及 usage、实际页面副作用、独立结束判据、APK/SHA/模型/任务版本与隐私清理记录。旧桥接基线只作对照。测试结束或失败后锁屏；真实 API、手机、凭据、费用与 Git 由主 Agent 独占。
