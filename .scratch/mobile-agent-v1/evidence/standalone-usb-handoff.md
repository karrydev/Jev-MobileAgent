# 任务 26：断开 USB 验收交接

2026-09-25。开发分支 `codex/v1-standalone-runtime` 的 `373da44` 已推送；最终 APK `800dc859a29f9a8990ded0c28abbfd4dd2fe0f8fedb514f671ed168956e8ac7e` 已安装。实现与当前缺陷修复经 Luna 开发、Sol 定向复审；[USB 联调与失败汇总](standalone-runtime-review01.md)。

## 已准备

- OnePlus 8T 已恢复无障碍权限，VLM Key 加密保存，Jev 配置可空；受控页输入为空，蓝色目标 ready，中文目标已选好。手机锁屏。
- 项目 Python 运行进程未发现，8765/18771 无监听，ADB reverse 为空，无线调试为 0；USB 尚未物理断开。
- 累计已知标价估算 ¥0.8226915；加已有未知用量/费用预留共 ¥1.8496385。另为下述两项各预留 ¥1，尚未调用，不得提前算作已执行。

## 唯一待人工操作

物理拔掉 USB、解锁后：

1. 点“开始本地 VLM 任务”，等待中文输入任务 SUCCEEDED。
2. 收起键盘，点“视觉手势目标”，再点“开始本地 VLM 任务”，等待 SUCCEEDED。
3. 锁屏、接回 USB，回复结果。若暂停、失败或约一分钟未结束，不重试，直接锁屏接回。

重新连接后由主 Agent 导出新增本地任务，核对 source/APK/model、请求/费用、动作/反思/新观察终局和实际页面，结算 `standalone-usb-input01` / `standalone-usb-visual01`。保留所有失败，关闭预算预留，不重置 App 累计。必要时补充证据，不能仅凭用户看到 SUCCEEDED 放行。

## 后续顺序

通过后任务 26 一次合入 main 并完成基线归档，解锁 22（Jev 影子建议）与 27（手机内中断恢复）。分别由新的 Luna-max 实现、Sol-max 按风险审查。当前不要领取被 26 阻塞的票，也不要把旧桥接恢复验收搬作新 App 证据。

2026-09-25 13:11 回连复核：两项均通过，预留已按实际请求结算，见 [独立基线](standalone-baseline.md)。不再等待人工操作。
