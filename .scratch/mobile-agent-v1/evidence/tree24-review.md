# 24 固定版本审查

2026-09-25，Sol-max只读审查 `4f8abde..d3a5f8c`；本记录由主协调整理。未运行真机或重复测试。

三项阻断：

1. `LocalTaskStore.reserveJevShadowCall` 按step拒绝第二个请求。该步选择请求已发送时，后续核验请求必被拒；需区分用途并维持每用途防重复、共享预算与计数。
2. 核验分支忽略 `budget_denied`，继续VLM回退；需在预算门禁拒绝时暂停。同时树核验开关独立启用时应能使用已配置且经过审查的Jev，不依赖选择/影子开关。
3. Debug自绘目标把结果同步到可访问TextView，不能作为“树外变化需视觉证据”的有效样例；仅Debug隐藏此结果的Accessibility暴露，保持屏幕可见。正常对照页面不改。

其余已审路径：UNKNOWN/PENDING在模型回退前保持未决落盘；等待最多两次；视觉分支要求真实前后图；迟到控制检查未发现阻断。结论是源码审查，不替代真实手机标签和配对测试。

主协调已暂停新的真实调用并锁屏，交新的Luna-max集中修复。`tree24-off-input01`为配置无效尝试，未用于放行；全部消耗保留。修复后定向复审这些行为和持久化边界，再开始正式配对。

## 定向修复复审

Luna-max修复提交 `8c112d5`；LocalTaskStore/JevShadow/预算常量三个测试类共14项通过，APK构建通过。新Sol-max对 `d3a5f8c..8c112d5` 定向复审：上述阻断均关闭，未见新代码阻断。已发送旧记录按source兼容，无法识别者保守拦截；硬预算拒绝在VLM前停止，动作进入NEEDS_REVIEW；仅Debug隐藏视觉结果。实际加载试验同一步已成功发送选择和核验两次请求。
