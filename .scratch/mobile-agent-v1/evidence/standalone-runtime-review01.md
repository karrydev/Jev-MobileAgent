# 任务 26：独立 App 初版审查记录

2026-09-25，按 code-this 由 Luna 实现、Sol 只读审查，主 Agent 集成及真机验收。开发分支 `codex/v1-standalone-runtime`，起点 `7e50ff1`。本记录对应修复前 APK `51dfb37fe1b0ecbad90527eb9341337ce762e06b82967b9cf3d8030e4b0c02dc`，不是最终放行结论。

## 配置与客户端

首次审查发现配置与密钥分开提交导致混配、改变端点时沿用旧 Key，以及根 URL 拼接双斜杠。迁移清理方法还需确保替代 Key 可读取。已集中修复：完整配置与密文原子保存、变更供应商或端点需输入 Key、根路径归一、保留无法迁移的旧凭据。Sol 定向复审无剩余阻塞。配置与客户端分别有 7/9 项 JVM 测试；真机保存、重载和迁移结果见 [settings-device01](standalone-settings-device01.json)。

复审指出根 URL 测试的假连接忽略了实际 URL 参数，不能检出未来回归；后续集中修复将改为断言工厂收到的 URL。该问题不改变本次源码归一行为正确的判断。

## 运行时审查发现

| 问题 | 当前路径与影响 | 修复边界 |
| --- | --- | --- |
| 反思拿到上一动作 | `reflectorPrompt()` 从尚未更新的历史取动作，第一步为空、后续滞后一条 | 反思前设置当前动作，验证提示词中当前动作正确；不提前伪造已核验历史 |
| 复合目标提前成功 | 关键词匹配把输入与发送的复合目标，只按输入结果放行；手势类型也未严格区分 | 完整识别支持的受控目标，未知或复合目标保持 UNKNOWN；目标类型须匹配 |
| 同窗口变化仍用旧坐标 | 滚动不改变窗口 ID/边界，旧观察版本仍可被接受 | 派发前检查现场有效性，拒绝已变化的目标；自有控制状态更新不应造成无意义失效 |
| 核验记录覆盖执行回执 | 反思 B/C 仍写成功回执，核验与历史分开落盘 | 分别保留执行事实与核验结果/错误，以持久化成功作为核验完成依据 |
| 视觉验收器过宽 | 灰色画布区域触摸也会标记蓝色目标完成 | 按实际目标范围和手势类型判定，增加未命中反例 |

这些均由本批实际调用路径或可构造反例支持，已交新的 Luna 集中修复。未将任务 27 的完整恢复界面或故障矩阵加入本次审查范围。

## 修复前真机结果的用途

[中文输入联调](standalone-input01.json)与[视觉点击联调](standalone-visual01.json)分别观察到精确输入文本和目标画布点击，真实调用均由 App 发出。视觉动作坐标为 `(540,996)`，位于本次页面蓝框内；仍须修正验收器的未命中判据。两轮共 14 次调用，标价估算 ¥0.085797，全部进入[费用台账](../live-model-budget.json)。

两轮为 USB 连接下的集成证据，且早于上述修复；不作为最终独立运行或策略收益基线。修复后根据受影响行为重新取证；已通过且源码未再改变的 Keystore 设置证据可沿用。每轮结束已锁屏。

## 集中修复与新实测发现

`7e83965` 已修复上述五项及根 URL 测试、反思分类严格解析；Sol 定向复审无剩余阻塞，受影响的七个测试类共 28 项通过并构建 APK。既有客户端和凭据证据按未变更范围沿用。

随后 [input02](standalone-input02.json) 实际输入精确中文，但模型按提示词选择 `answer` 后，Android 解析器拒绝该动作，最终因五步上限暂停。这是新实测暴露的移植缺陷，不能把物理输入成功当作任务闭环成功。已交新的 Luna 修复回答动作语义，仍要求独立终局核验，不提高步数掩盖问题。本轮 10 次调用标价估算 ¥0.062508，已结算台账；手机已锁屏。
