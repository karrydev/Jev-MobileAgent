# 原始目标限定在规划和执行中的保留

2026-09-26。真实 on-visual08 中，原请求要求蓝色视觉目标，Manager 首份计划却改成点击同名“视觉手势目标”按钮；Executor 收到原请求和这个冲突计划，最终执行灰色任务预设按钮。实际蓝框仍ready，视觉守卫拒绝静态误报并暂停。该误认也在历史OFF发生，不据此归因于规划开关。

Luna-max 仅修改 MobileAgentVlmRoles 角色提示与对应回归测试：初次规划、重规划持续保留用户限定；名字相似只是部分证据，配置/预设/说明与用户所指内容不同；Executor 对照原请求检查计划，冲突时原请求优先，不能把近似目标当作已取得进展或完成。没有硬编码受控页面、颜色名、坐标或答案，不改候选选择、预算、核验、动作schema或两种坐标协议。

初稿提出的空Action已在审查前删除：真实Service解析异常后仍可能通过Jev候选回退，因此不是安全暂停。最终补丁保留现有合法JSON动作格式。新增测试仅验证这些约束被带入三个角色提示入口，不能证明模型遵守。

最终20项定向检查通过（MobileAgentVlmRolesTest 15、VlmCoordinateProtocolTest 5），详见[测试元数据](request-grounding-tests.json)。独立Sol-max定向审查及最终APK版本随后补充；真实效果尚未验证。此前有效的取消/视觉守卫测试与真机结果未被这个提示词补丁替换。

真实复测当前受费用门禁限制：planning25阶段已计¥1.0226505，阶段¥2剩¥0.9773495，少于每任务¥1完整预留。累计¥10与每任务¥1上限未改，Jev72/150；不清历史、不改阶段归属、不擅自增限或降低冻结任务预算来绕过。先完成可审查补丁、构建和准备，再由用户决定额外复测授权。25/27/28状态保持未完成。

独立Sol-max最终审查CLEAR：原请求通用限定、合法设置任务例外、合法动作JSON及两种坐标协议均保留；无范围内阻断意见。20项定向测试通过，git diff --check通过。真机效果仍待预算许可，不据静态审查关闭25。

最终候选源码3605af4f10997778585cf3b79e7a9f0575cc4b56，APK SHA-256 c3f68f3d28c7bb60d582ee4875e9dc0fd5253bf4625fa1354144ef19a1bf0a4e；assembleDebug 8s成功，原Android Debug签名验证通过，install -r成功，手机base.apk哈希一致。升级后无活动任务，Jev72、费用与历史保留、规划OFF，Dozing。已向用户请求仅将planning阶段上限¥2→¥3供四项复测，其他上限不变；答复前不改预算或启动API。详情见[候选构建](request-grounding-candidate-build.json)。
