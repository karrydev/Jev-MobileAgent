# 25 按需规划配对验证计划

2026-09-25，首个任务25真实模型调用前冻结。沿用24正常配对目标与独立标签，不用27恢复专用页面代替正常配对。最终25/27集成源码与同一APK上各运行规划OFF/ON，开关之外设置一致；最终构建SHA和APK哈希随证据写入。

设备 OnePlus8T / Android14；App加密保存 GUI-Plus gui-plus 和 TypeSafe jev-1.13.0。Jev选择ON、树核验ON、影子OFF，任务上限5步/25 VLM/5 Jev/¥1，原全局Jev40次和¥10预算保留。25阶段新增费用与未知预留最多¥2，每次先预留，全部失败、取消、未知usage计入，不重置历史。四个正式配对任务按输入OFF、输入ON、蓝框OFF、蓝框ON顺序各一次；只有具体修复或准备环境变化才新增编号复测，原尝试不覆盖。

中文输入目标为“在中文输入框中输入‘独立手机测试成功’”（实际采用App预设中文双引号），初始目标框空；蓝框采用正常受控页已有预设目标与复位页面。两边同一竖屏、软键盘收起、相同初始状态。协调者只准备并启动，不替模型完成动作。实际输入内容/蓝框变化、动作回执、每步核验与整体任务结论分别记录。目标和调用减少不能互相替代：ON不得新增错误动作、误报成功、提前结束或降低完成质量，否则保持默认关闭且票不宣称通过。

记录任务结果、全角色请求、计划refresh/reuse原因、图像采集/上传、耗时、已知费用与未知预留。对照只评价本小样本，不外推普遍性能收益。未触达的循环、重复失败、过期计划、预算耗尽等分支由相应离线策略/门控测试说明范围，不以模拟证明真实任务覆盖；发现真机路径缺陷后再针对补测。

默认开关OFF的原提示和原shouldSkipManager调度为对照。逐步核验保持；子目标完成提示只有实际核验SUCCESS才可触发重新规划，UNKNOWN仍暂停，不从模型自述或回执推断成功。恢复后仍需fresh核对和用户确认，预算与计划事件保留。

主协调独占真机、真实API、凭据、预算和Git。任何结束/失败后锁屏。USB只用于准备与取证，本配对不代替28最终物理拔线验收。

2026-09-25 首次25配对调用前按用户要求将本轮两侧模型统一为 gui-plus：日期版返回免费额度耗尽403，gui-plus已实探200。新配对只比较同APK、同gui-plus下的调度，不把与24日期版模型之间的差异解释为规划收益。费率仍为输入¥1.5/百万token、输出¥4.5/百万token（[官方价格](https://help.aliyun.com/zh/model-studio/gui-automation)）；原费用和全部失败保留。

2026-09-25预算修订：达到此前40次Jev上限后停止相关调用，用户明确回复“提高到70次，费用上限不变”。新全局上限70，已用40次保留，累计¥10/每任务¥1/阶段¥2/每任务5次Jev不变。追加额度用于修复后4项配对与最终2项独立运行，不追溯改变前述冻结配置或费用记录。

2026-09-25追加授权：用户明确“Jev调用扩充到总共150次”。由70提高为150，已用46保留，累计¥10、每任务¥1、阶段¥2及每任务5次Jev均不变。所有历史尝试和费用继续计入。
