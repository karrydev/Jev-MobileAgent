# 13 — 提供真实 VLM 最小访问条件

**What to build:** 用户在探针与配置样例就绪后提供服务端环境中的 VLM 凭据和允许的试跑预算，使真实协议验证可以进行。

Blocked by: none

Status: ready-for-human

Execution: done
Owner: unassigned
Branch: unassigned
Evidence: 用户访问条件
Gate: 提供真实 VLM 最小访问条件

## 前置与规格

无技术前置；人工项按触发时机请求。

[首版规格](../spec.md) · [任务索引](../index.md) · [并行协作](../../../docs/parallel-development.md)

## Acceptance criteria

- [x] 凭据只写入约定的本地运行环境；确认 endpoint、模型身份和最小试跑费用边界，不在聊天或仓库公开 key。
- [x] Agent 已准备配置样例与探针，用户仅处理账号开通、必要支付或访问授权。
- [x] 本项完成仅说明访问条件到位；真实调用成功由任务 15 证明。

## 用户参与触发条件

任务 09 探针就绪且准备开展真实 VLM 检查时触发；尽可能与任务 14 合并一次沟通。
无技术前置不表示现在请求用户；协调者仅在上述时机准备好具体材料后一次性请求。

## 交付证据

记录最终合入 SHA、验证命令、契约与环境版本、实际结果和未验证项。模拟证据只关闭模拟任务；真实能力缺失时不能记为通过。共享接口变更由集成负责人串行合入。

## Comments

2026-09-22：用户已确认测试边界、任务拆分与延后人工准备；本票已发布，尚未实施。

2026-09-22：Agent 准备项已就绪：`services/sim_loop/README.md` 包含完整 chat-completions endpoint、model、`JEV_VLM_API_KEY` 环境变量名与 probe 命令；不加 `--execute` 仅检查配置，不发请求。任务 09 的本地供应商夹具已覆盖多图、中文、usage 和错误，主树集成回归通过。仍延后请求用户 endpoint/model、运行环境中的凭据及有限试跑预算，待 08 设备截图/手势切片完成后集中沟通；未读取真实凭据或调用供应商。

2026-09-23：08 受控真机切片已合入，01–12 可自主推进的切片全部完成。现在首次请求用户提供完整 endpoint、model、本地凭据位置/环境变量名与首轮试跑预算（建议上限 ¥10）；请求已准备好的真实协议探针所需条件，不要求提前准备 Jev 或服务器。访问和预算尚未收到，15 及其依赖保持未开始；没有发送真实模型请求。

2026-09-23：用户确认首轮实际付费总预算 ¥10，并指定后续新增模型优先采用百炼托管、账号仍有免费额度且满足任务协议的模型；不预设所有模型均有额度，也不将第三方专用模型自动替换为不等价能力。首轮模型为 `gui-plus-2026-02-26`（北京地域）。用户已提供仓库外的本地凭据文件位置，但当前进程读取返回 `Operation not permitted`；未读取 Key、未发起付费请求，访问门禁仍未闭合。用户要求正式产品支持 App 内填写供应商 Key，当前设备桥 Token 不等于模型 Key；该配置入口与服务端凭据处理尚未实现，不能报告为已有能力。

2026-09-23：用户将配置移到仓库外可读位置并授权继续；已读取非空 JEV_VLM_API_KEY，仅进程内使用。配置固定北京兼容 endpoint `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`、`gui-plus-2026-02-26`，首轮付费上限 ¥10。一次真实双图/中文访问检查 HTTP 200、输出“红色，蓝色”、usage=156 输入+4 输出；公开价格折算 ¥0.000252，实际免费抵扣/账单未查询。首次本机 Python CA 缺失在 TLS 阶段失败，采用已安装 certifi CA 验证后成功，未关闭证书校验。访问条件已满足；角色协议验收仍由15完成。
