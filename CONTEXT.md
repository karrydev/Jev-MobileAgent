# Jev-MobileAgent

帮助用户在 Android 手机上完成跨应用目标的自动操作系统。以下词汇统一需求、实现讨论与验收的含义。

## Language

**独立 App（Standalone App）**：用户安装并配置模型凭据后，仅凭手机及互联网即可发起、执行、核验和恢复任务的产品；使用不要求开发电脑或用户另行部署服务。
_Avoid_: 将“手机能连接远端设备桥”称为独立 App

**任务（Task）**：用户希望达成的完整目标，具有起始条件和整体完成条件。
_Avoid_: 用“动作成功”代指任务完成

**子目标（Subgoal）**：任务当前要达成的中间目标，可以需要多个动作才能完成。

**观察（Observation）**：某一时刻设备向系统提供的界面证据，包含可访问的语义和可获得的视觉内容。
_Avoid_: 完整内部布局、全量 DOM

**观察版本（Observation version）**：区分观察所代表现场的标识；它限定从该观察作出的选择仍适用的范围。

**节点引用（Node reference）**：某次观察中一个可访问界面元素的临时身份，不是跨页面永久标识。

**候选动作（Candidate action）**：目标与参数已经齐备、可供选择的一个操作方案；选择它不需要再生成未知参数。
_Avoid_: 只有动作类型而没有目标或输入内容的“完整候选”

**执行回执（Execution receipt）**：设备对命令受理或执行过程的事实反馈，不代表动作已经达到预期效果。

**动作后置条件（Action postcondition）**：判断某次操作是否达到预期效果的条件，与整个任务的完成条件分别定义。

**动作核验（Action verification）**：根据操作前后证据判断动作后置条件，结论为 SUCCESS、FAILURE、PENDING 或 UNKNOWN。

**SUCCESS**：已有证据表明本次动作的预期后置条件满足。

**FAILURE**：已有证据表明本次动作未达到预期后置条件。

**PENDING**：已有证据表明结果仍在等待中，需要在有限时间内重新观察。

**UNKNOWN**：现有证据不足以判断；不能被解释为成功或直接等同于失败。

**任务完成（Task completion）**：整个用户目标的完成条件满足，不由某一个动作的回执或核验结果单独决定。

**视觉回退（Visual fallback）**：树中信息不足时，使用截图与生成式模型继续选择动作或核验结果。

**接管（Takeover）**：自动执行暂停后，用户检查现场或亲自操作的过程；继续自动执行需要重新确认现场。

**恢复（Resume）**：用户明确确认后，在重新观察并核对执行历史的基础上继续原任务。
_Avoid_: 将重连、重复发送旧动作称为恢复

**基线（Baseline）**：代码、模型、入口、任务和环境均被记录的对照运行；不同入口分别建立基线。

**影子运行（Shadow run）**：记录新策略的建议而由既有策略控制设备，用于比较建议，不能证明新策略执行后的任务成功率。

**独立判分（Independent evaluation）**：依据任务外部真值或人工准则判断结果，不把 Agent 自己的判断当作正确标签。
